0、在发送了报告后客户端崩溃，流程驻留在内存中，这个时候falres不正常，并且假如服务器自检没有检测出来的话会出现错误码异常
1、禁止客户端使用同一个devsn登陆；强制登陆的话，应当延迟挂入链表，仅当绑定关系通过后才允许登陆
2、当征信账号密码错误时，提前替用户锁定，除非采用不同的密码重新登陆，或者管理员在网站上重新确认密码可用
2、业务维持由于流程在客户端离线后无论如何都会被结束，因此在当前版本下不可用
2、从管理网站上踢除某个设备的连接状态
3、已注册过的Ukey与TPM如何保证不重复注册
4、在检验Ukey证书时，应该同时让客户端传一串用他私玥签名的字符串，服务器用他的公钥验签，以此保证对端是拥有一整套证书的。而并不是像现在这样只检测对端证书的合法性
5、应该将ukey证书、tpm证书、ukey设备、tpm设备、机柜五个元素都关联起来，而非现在只关联tpm-ukey-dev这样。
11、密码自动修改函数复杂化，8～20个字符，至少包含'0'~'9'、'A'~'Z'、'a'~'z'、'-_@#'中两种或者两种以上的字符
11、征信信息在存储时也使用服务器公钥加密后再用Ukey公钥加密存储，这样保证在客户端中不会存在明文


已知bug：
1、配置文件中DATA_PATH超长会引发退出时异常
2、子进程频繁退出后会导致各种段错误
